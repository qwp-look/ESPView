# Normalize line endings for text files (LF in repo, CRLF on Windows checkout)
* text=auto

# Binary assets
*.png binary
*.bin binary

# cmd.exe requires CRLF .bat files on every checkout (incl. Linux CI)
*.bat text eol=crlf
